#include <stdio.h>

int main()
{
    int m;
    int n;
    printf("Enter the number of rows that you want:\n");
    scanf("%d", &m);
    printf("Enter the number of columns that you want:\n");
    scanf("%d", &n);

    // double myArr[m][n];
    // for (int j = 0; j < n; j++)
    // {

    //     for (int i = 0; i < m; i++)
    //     {
    //             double temp;
    //             printf("Enter the value for row %d, column %d:\n", i, j);

    //             scanf("%lf", &temp);

    //             myArr[i][0] = temp;
    //     }
    // }
   
    
    
    


    // printf("Here is the value you entered: %d\n", m);
    return 0;
}